# Forge Scanner — Dependency Audit Baseline
**Date:** 2026-04-23  
**Tools:** `bunx depcheck`, `bunx bun-audit`  
**Note:** `osv-scanner` is not available as an npm package (404 on registry). `bun audit` used as replacement via `bun-audit`.

---

## 1. Unused Dependencies (depcheck)

### Unused runtime deps (candidates to remove after UI confirmation)
| Package | Reason |
|---|---|
| `@react-email/components` | No import found in `src/` |
| `@stripe/react-stripe-js` | No import found in `src/` |
| `@stripe/stripe-js` | No import found in `src/` |
| `class-variance-authority` | No import found in `src/` |

**Action required:** Verify these aren't lazily loaded via dynamic imports or Next.js middleware before removing. If confirmed unused, remove in a separate PR.

### Unused devDeps (candidates to remove)
| Package | Reason |
|---|---|
| `@tailwindcss/postcss` | No postcss config found importing it |
| `@types/react-dom` | No import found |
| `supabase` | CLI — only needed locally, not in CI |
| `tailwindcss` | Possibly unused if using Tailwind v4 via CSS import |

### False positives (depcheck cannot see these)
| Package | Why depcheck misses it |
|---|---|
| `@remotion/google-fonts`, `remotion` | In `.claude/skills/` (not `src/`) — depcheck false positive |

---

## 2. Known Vulnerabilities (bun-audit)

**3 vulnerabilities found across 615 packages audited.**  
**Severity: 1 High, 2 Moderate**

### HIGH — Next.js DoS via Server Components
| Field | Value |
|---|---|
| Package | `next` |
| Current | 16.2.0 |
| Patched in | >=16.2.3 |
| Advisory | https://www.npmjs.com/advisories/… |
| Action | **Bump `next` to 16.2.3 ASAP** — DoS is a production risk |

### MODERATE — @anthropic-ai/sdk Memory Tool Path Sandbox Escape
| Field | Value |
|---|---|
| Package | `@anthropic-ai/sdk` |
| Current | 0.80.0 |
| Patched in | >=0.81.0 |
| Advisory | https://www.npmjs.com/advisories/1116375 |
| Action | Bump to 0.81.0; verify no breaking prompt-caching changes |

### MODERATE — uuid Buffer Bounds Check (transitive via resend)
| Field | Value |
|---|---|
| Package | `uuid` (transitive: `resend > svix > uuid`) |
| Patched in | >=14.0.0 |
| Advisory | https://www.npmjs.com/advisories/1116970 |
| Action | Wait for `resend` upstream to bump svix; no direct fix available |

---

## 3. Recommended Next Steps

**This week:**
1. `bun add next@16.2.3` — HIGH vuln, 3-line patch release, minimal risk
2. `bun add @anthropic-ai/sdk@0.81.0` — MODERATE, worth doing alongside next bump

**When confirmed unused:**
3. Remove `@react-email/components`, `@stripe/react-stripe-js`, `@stripe/stripe-js`, `class-variance-authority` from dependencies (after UI audit)

**Defer:**
4. `uuid` moderate — transitive, fix blocked on resend/svix upstream

---

*Generated by Kova — audit-only, no deps auto-removed per QW10 directive.*
