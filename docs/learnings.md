# Project Learnings

> Error patterns discovered during audits. Cody reads this on boot and before dispatching agents.
> When a pattern appears 3+ times in FIX-LOG → add an entry here.
> If a pattern appears in 2+ projects → update the vault agent identity at `../../build/agents/[name]/CLAUDE.md`.

<!-- Entry format:

## [ISO date] — [Pattern name]
**Found in:** AUDIT-[NNN], [BUG/RUNTIME/MISMATCH]-[N]
**Agent:** [backend / frontend / ai-engine]
**Pattern:** [What went wrong — the recurring behavior, not the one-off bug]
**Root cause:** [Why this keeps happening]
**Prevention:** [What instruction change prevents this]
**Applied:** [yes/no — was the agent instruction updated?]

-->

_No learnings yet._
